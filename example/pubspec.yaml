name: dchs_flutter_compass_example
description: Demonstrates how to use the dchs_flutter_compass plugin.
version: 1.0.0+2

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  dchs_flutter_compass:
    path: ../

flutter:

  uses-material-design: true

  assets:
    - assets/compass.jpg